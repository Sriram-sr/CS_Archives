Chapter Summary

    All HTML elements can have attributes
    The href attribute of <a> specifies the URL of the page the link goes to
    The src attribute of <img> specifies the path to the image to be displayed
    The width and height attributes of <img> provide size information for images
    The alt attribute of <img> provides an alternate text for an image
    The style attribute is used to add styles to an element, such as color, font, size, and more
    The lang attribute of the <html> tag declares the language of the Web page
    The title attribute defines some extra information about an element

<hr> tag to straight line

<a href="#">any name</a> => This link will move to top of page
<h1 style="font-size:60px">any text</h1> or style="font-size:300%"
    To change or increase size of h1 or any tags
<pre> tag used for correct alignment
<body style="background-color:anycolor">
   colors the body
<h2 style="color:blue">any text</h2>
   To color only specified header
<p style="font-family:courier">any text</p>
   to add css font families
<p><em>any text</em></p>
   to print text emphasized
<p><small>any text</small></p>
<p>any text<mark>any text</mark></p>
   highlights the text inside
<del>Any text</del>
   will strike out text inside
<u>any text</u> (or) <ins>any text</ins>
  will underline text
<blockquote>any text</blockquote>
  will indent the text inside blockquote
<q>text</q> => add quotes
<abbr title="Indian Premier League">IPL</abbr>
  will return the title when you mousehower the abbreviation
<a href="anyLink" target="_blank">anyName</a>
  will open the link in new tab

To make a text centered you can use style attribute ,
<p style="text-align:center/left/right"> any text</p>

<html> </html>  This is called container tag as it has both opening and closing / tag
Html document itself a containered opening

<a href="somelink"> 
This complete is html element
here href is a attribute(attribute inside tags)
